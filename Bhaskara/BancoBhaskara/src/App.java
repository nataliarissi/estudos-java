import java.util.Scanner;

public class App {
    public static void main(String[] args) throws Exception {
    Scanner scanner = new Scanner(System.in);
    // System.out.println("Digite um número para ser o A: ");
    // double a = scanner.nextDouble();
    // System.out.println("Digite o número para ser o B: ");
    // double b = scanner.nextDouble();
    // System.out.println("Digite o número para ser o C: ");
    // double c = scanner.nextDouble();
    
    // Bhaskara bhaskara = new Bhaskara(a, b, c);

    // System.out.println(bhaskara.toString());
        int n;
        System.out.println("Digite os números: " + n+1);
        int algoritmos = scanner.nextInt();
        
        int numeros[] = new int [algoritmos];

        for(n=0; n<algoritmos ;n++){
            if(n<algoritmos)
            System.out.print(numeros[algoritmos]);
        }

        

    for(n=0; n>n; n++){
        System.out.println("a" + numeros[n] +1);


    }Para adicionar coisas em um array: array[0] = 1;
        // System.out.println("Digite os números: ");
        // int algoritmos = scanner.nextInt();
        // int numeros[n];
        
        // int numeros[int n] = n;
        // n.add numeros[algoritmos];
        // System.out.println("a" + numeros[n] +1);
    }
}

    print("a", mat[n], +1);
    print("a"+ mat[n]+ +1);

Criar um programa que leia uma quantidade x predefinida de numeros que o usuário digitar e armazene esses numeros em um array

instanciar a classe passando esse array
Para adicionar coisas em um array: array[0] = 1;
// Criar uma classe que irá Processar números

// A classe deverá receber um Array de números ao ser instanciada

// A classe terá uma funcao chamada EncontrarMaiorNumero que retornará o maior número dentro do array

// A classe terá uma funcao chamada EncontrarMenorNumero que retornará o menor número dentro do array

// A classe terá uma funcao chamada ObterMediaNumeros que retornará a média entre os números 
// (a média é definida pela seguinte equação: todos os números somados dividido pela quantidade de números)